<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.com.yusys.yusp.oca.dao.AdminSmDutyUserDao">


    <select id="pageDutyUserByDutyId" resultType="cn.com.yusys.yusp.oca.domain.vo.AdminSmUserDutyRelVo">
        SELECT
            ud.DUTY_ID,
            o.ORG_NAME,
            u.LOGIN_CODE,
            u.USER_NAME,
            u.USER_CODE,
            u.USER_STS
        FROM
            admin_sm_user u
                LEFT JOIN admin_sm_user_duty_rel ud ON ud.USER_ID = u.USER_ID
                LEFT JOIN admin_sm_org o ON o.ORG_ID = u.ORG_ID
            ${ew.customSqlSegment}
    </select>


</mapper>