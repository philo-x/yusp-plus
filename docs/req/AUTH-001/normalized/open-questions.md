# AUTH-001 待决问题

| TBD | 问题 | 责任方 | 关闭条件 |
|---|---|---|---|
| TBD-SEC-001 | 多角色叠加、跨组织兼职和临时授权的合并顺序、冲突处理、有效期是什么？ | 安全、业务 | 签署角色×组织×对象判定表，覆盖允许、拒绝、冲突及到期场景。 |
| TBD-INT-001 | 组织主数据使用文件还是 API，唯一标识、字段、增量方式和调度时间是什么？ | 数据仓库、架构 | 组织主数据契约、样例、调度、错误与补偿方案通过评审。 |

关闭任一问题时，必须同步更新需求、规则、验收和来源映射。
